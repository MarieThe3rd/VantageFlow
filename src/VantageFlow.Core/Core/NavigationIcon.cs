namespace VantageFlow.Core;

/// <summary>
/// A NavigationItem's icon, kept independent of any specific UI framework's icon type so this
/// project can stay free of WinUI dependencies. The shell maps each value to a real icon.
/// </summary>
public enum NavigationIcon
{
    List,
}
